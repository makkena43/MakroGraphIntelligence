# Document Fetching
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.2.0

# PDF Parsing
pdfplumber>=0.10.0
PyMuPDF>=1.23.0

# Text Processing
ftfy>=6.1.0
unidecode>=1.3.0
chardet>=5.2.0

# Storage
# SQLite is built-in; FTS5 included in Python's sqlite3

# Configuration
PyYAML>=6.0.0

# Hashing (for dedup)
xxhash>=3.4.0

# Logging & Utils
tqdm>=4.66.0
python-dateutil>=2.8.0
schedule>=1.2.0

# -----------------------------------------------
# Intelligence System Dependencies
# -----------------------------------------------

# PostgreSQL + pgvector
psycopg2-binary>=2.9.9

# Neo4j graph database
neo4j>=5.17.0

# NLP: spaCy (run: python -m spacy download en_core_web_sm)
spacy>=3.7.0

# NLP: Financial NER (FinBERT)
transformers>=4.38.0
torch>=2.2.0

# Semantic Embeddings
sentence-transformers>=2.6.0

# Topic Modeling
bertopic>=0.16.0
scikit-learn>=1.4.0

# Temporal Knowledge Graph
graphiti-core>=0.3.0

# LLM Clients (optional - only needed if llm_enabled: true)
# DeepSeek uses the openai package with base_url override (set DEEPSEEK_API_KEY)
anthropic>=0.25.0
openai>=1.12.0

# Numerical / ML utils
numpy>=1.26.0
pandas>=2.2.0

# FastAPI backend (React UI)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# -----------------------------------------------
# India Market Ingest Dependencies
# -----------------------------------------------

# HTML scraping (BSE, Screener, InvestIndia, Commerce)
beautifulsoup4>=4.12.0
lxml>=5.1.0

# RSS feed parsing (PIB India)
feedparser>=6.0.0

# NSE India API wrapper (optional but recommended)
# Handles NSE session-cookie management automatically
nsepython>=2.9

# Selenium (optional — only needed if screener.use_selenium: true)
# Also requires ChromeDriver matching your Chrome version
selenium>=4.20.0

# Presentation parsing (investor decks, annual reports)
python-pptx>=0.6.23

# OCR for scanned PDFs (optional — only needed for image-only annual reports)
# Requires system-level install: brew install ocrmypdf  (macOS)
# ocrmypdf>=15.4.0
